import { Flex, Heading } from '@chakra-ui/react'
import React from 'react'

function SideBar({ label }) {
  return (
    <Flex height={'100%'} width={125} bg={'primary.main'}>
    </Flex>
  )
}

export default SideBar